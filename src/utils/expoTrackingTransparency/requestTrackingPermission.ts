import { AppStateStatus } from 'react-native';

//Cas web => voir expoTrackingTransparency.native.js pour android et ios
export const _requestTrackingPermissions = (appState: AppStateStatus) => {
  return;
};
